# pytex render service runtime deps (T-21).
# pytex pinned exactly for a reproducible image; the web layer floats on minors.
fastapi>=0.115
uvicorn[standard]>=0.34
# starlette parses the `multipart/form-data` shape of POST /render with this.
python-multipart>=0.0.20
pytex-preprocessor==1.1.0
