# pytex

Render-Service: FastAPI-Wrapper um `pytex_api.render_blob` (Markdown → PDF, trusted,
Variante via Frontmatter). pytex v1.0.0.

**Status:** Skelett (T-01) — nur `/health` + `/render`-Stub (501). Reale Anbindung +
tectonic-Cache-Volume in **T-21**.
