# frontend

Angular-SPA (TS strict), @ngx-formly (dyn. Formulare), rete.js/@foblex/flow
(Flow-Editor), RxJS (WS-Streams). Design-System aus HSRT-Palette.

**Status:** Platzhalter (T-01). Skelett + Design-System (CD-Tokens, Light/Dark,
Font, Logos, core) folgt in **T-03**.

Im Skelett-Deployment serviert `web` (nginx) bis dahin eine Platzhalter-`index.html`
(`deploy/web/html/`).
