---
name: backlog-2026-06-13
description: 2026-06-13 backlog session (18-item list) — progress + decisions; branch fix/gremium-member-perms
metadata: 
  node_type: memory
  type: project
---

User's 18-item backlog (pasted 2026-06-13). Working on branch **fix/gremium-member-perms** (off main). Commit between every change. Rule: **ask on EVERY design decision** (AskUserQuestion), no assumptions.

**Done + committed:**
- **#5-3** (`fd06b38`): gremium-members empty table — memberships GET/POST/DELETE → admin.gremien (was admin.roles); GET gremium-roles + principals → ANY-of(gremien,roles). FE error-handlers.
- **#5-2** (`22c4248`): FE-guard↔BE-read perm sync. Relaxed reads to ANY-of incl. page perm: global-flow(+flow.configure,+budget.structure), admin/roles(+flow.configure), webhooks(+flow.configure), deadline-policies(+flow.configure), application-types(+form.configure), /expenses(+structure,+book). New **GET /accounts/options** (id+name, no IBAN) for budget.book|view. forms-list→authed /gremien. Decision: relax reads (writes stay strict); accounts via minimal options endpoint (IBAN stays account.manage).
- **#2 root cause fixed** by #5-2: Bankkonto dropdown now loads for bookers (settable at creation). Open: should account be editable AFTER creation? (BE ExpenseUpdate only allows amount/description.)
- **#5-1** (`ca48f18`): admin landing tiles now filtered by route-guard permission via auth.canAny (hide, not disable — user said "verstecken").

**Pattern reused:** relax BE READ endpoint to ANY-of incl. consuming page's permission; writes stay strict. See [[admin-domain-rules]].

**Done + committed (cont.):**
- **#1-1/#1-2/#2/#3/#4** Buchungen-Felder: BE `14d3d1c` (migration 0023, BudgetExpense + invoice_date, payment_date, correspondent='Empfänger/Zahler', note, reference_number=Belegnummer, payment_method enum, category; all nullable + editable), FE `0257fe4` (table: Rechnungs-/Zahldatum sortable cols default invoiceDate + Empfänger/Zahler col, hide created_at; create+edit dialogs all fields incl Bankkonto). Decisions: label "Empfänger/Zahler"; default sort Rechnungsdatum; all metadata+account editable; extra fields Belegnummer/Zahlungsmethode/Kategorie.

**Done + committed (cont.):**
- **#6** `365b63d`: datepicker host block + min-width:0 → no overflow/scroll in narrow dialogs.
- **#15** `4dd511a`: dialog body vertical padding (--space-1=4px) so focus ring not clipped at edge. (Chose: pad clipping containers. User should eyeball.)
- **#17** `1d93555`: formly checkbox align-items:center + margin:0 → equal vertical margin. (User should eyeball.)
- **#16** `eb96123`: roles expanded-row .roles__nameEdit grid (Key/DE/EN even cols, Save own footer line), evened cramped gaps. (Layout option B, user-approved.)

**Done + committed (cont.):**
- **#9** `961a163`: data-table empty state centered on mobile (parity w/ Anträge). Desktop already matched.
- **#14** `8f4e695`: mobile dialog — body scroll-lock, grabber + flick-to-close (drag grabber/header, velocity>0.5 or >120px), safe-area flush bottom. Tap-outside already worked.

**Done + committed (cont.):**
- **#10** `76ee1b0`: Sitzungen timeline mask-fade top/bottom + slim scrollbar.
- **#7** `424a855`: substitute pool sees committee session timeline (BE `_visible_gremium_ids` unions pool gremien; whoami `in_substitute_pool`; FE gate). Live channel still needs membership/delegation. Decision: timeline-visibility only, not live.

**#5-4 audit done** ([[be-fe-field-gaps-2026-06-13]]). Acting on gaps (user picked all 3 categories):
- DONE `be5a898`: agenda/assignable application-state labels shown.
- DONE `8c30f4c`: open-live-vote dialog now sets majorityRule + eligibleCount + quorumPercent (BE already consumed them).
- TODO display gaps (small, scattered): Expense actor/accountName/transferId cols, MeetingOut.closedAt, AttendanceOut.source, VoteOut.opensAt/eligibleGroup, VoteConfig.tieBreak/abstainCountsQuorum, ProtocolOut.sentAt, TimelineEventOut.actor, FormVersionOut.version.
- DONE `dc370c6`: **GroupMapping page** (/admin/group-mappings, perm admin.roles) — table + add/edit dialog + delete; added missing BE DELETE endpoint. Full FE.
- DONE `be5a898` agenda state labels; `8c30f4c` open-vote majority/quorum config.
- DONE `1a7f894`: **MailTemplate editor** (/admin/mail-templates, perm admin.notifications) — list + DE/EN tabs (subject/body/html) + placeholder ref + live preview. Service CRUD existed; added templates_router (GET/POST/PATCH + /preview) in main. Full FE. NOTE: preview renders the SAVED template (BE renders by id), not unsaved edits.
- DONE `7b73270`: **comparisonOffers config** in form-editor (required + minCount; preserves threshold/as). FE-only (BE already accepted it).
- **ALL 3 missing #5-4 features done.** TODO only: small display gaps (see [[be-fe-field-gaps-2026-06-13]]).

**Still open:** #8 ([[budget-import-zugferd]]), #11 (wiki), #12 (security audit — ultracode/workflow candidate), #13 (MCP — scope), #18 (adapters — design Q).

**Session total: ~20 items, 21 commits on branch fix/gremium-member-perms (off main, NOT pushed). Full suites green: BE 1443, FE 582.** Pending deploy: **migration 0023** + stack rebuild. User should eyeball #14/#15/#17/#10 on a phone. Pre-existing (not mine) ruff nit: protocol/service.py:26 import sort.
**#5-4 display gaps DONE (meaningful):** `0f09f55` expense account/transfer/booked-by; `94ed2fe` timeline actor + form version; `ee0b0b2` attendance source. Marginal ones deferred (vote eligibleGroup/opensAt, VoteConfig tieBreak/abstainCountsQuorum, MeetingOut.closedAt [not on FE type], StateOut.kind, ApplicationOut.lang).
**#12 security audit DONE:** findings + status tracked privately (not in this repo). Code fixes landed on the relevant branches.
**Build fix `35bacf9`:** ng build budget — see [[ng-build-budgets]].

**Still open backlog (all need scope/design/research or external repo):** #8 ZUGFeRD ([[budget-import-zugferd]] — lib research + drag-drop), #11 wiki (external …wiki.git), #13 MCP (needs scope from user), #18 adapters (design Q: which).

Stack: BE FastAPI/SQLAlchemy/Alembic `backend/app/modules/*`; FE Angular `frontend/src/app` (pages/, features/); MCP python. Expense model = `backend/app/modules/budget/tree_models.py:160` BudgetExpense.
