---
name: backlog-2026-06-13
description: 2026-06-13 backlog session (18 items) — progress and decisions on branch fix/gremium-member-perms
metadata: 
  node_type: memory
  type: project
---

The user pasted an 18-item backlog on 2026-06-13. Work runs on branch
**fix/gremium-member-perms**, branched off main. Commit between every change. Rule:
**ask about every design decision** with AskUserQuestion. Make no assumptions.

**Done and committed:**
- **#5-3** (`fd06b38`): the gremium-members table stayed empty. Memberships GET, POST and DELETE
  now need `admin.gremien` instead of `admin.roles`. GET gremium-roles and GET principals now
  need ANY-of(gremien, roles). The frontend got error handlers.
- **#5-2** (`22c4248`): the frontend guard permissions now match the backend read permissions.
  Reads relax to ANY-of, including the permission of the consuming page: global-flow
  (+flow.configure, +budget.structure), admin/roles (+flow.configure), webhooks
  (+flow.configure), deadline-policies (+flow.configure), application-types (+form.configure),
  /expenses (+structure, +book). New **GET /accounts/options** returns id and name without the
  IBAN for budget.book or budget.view. The forms list now calls the authenticated /gremien
  route. Decision: relax the reads and keep the writes strict. Accounts come from a minimal
  options endpoint. The IBAN stays behind account.manage.
- **#2 root cause fixed** by #5-2: the bank account dropdown now loads for bookers, so they can
  set the account at creation. Open question: may the account change after creation? The backend
  `ExpenseUpdate` allows only amount and description.
- **#5-1** (`ca48f18`): the admin landing tiles now filter by the route-guard permission through
  `auth.canAny`. The user asked to hide the tiles, not to disable them.

**Reused pattern:** relax a backend read endpoint to ANY-of, including the permission of the
consuming page. Writes stay strict. See [[admin-domain-rules]].

**Done and committed (continued):**
- **#1-1, #1-2, #2, #3, #4** booking fields. Backend `14d3d1c`: migration 0023 adds
  `invoice_date`, `payment_date`, `correspondent` (UI label `Empfänger/Zahler`, the payee or
  payer), `note`, `reference_number` (UI label `Belegnummer`, the receipt number), a
  `payment_method` enum and `category` to `BudgetExpense`. Every new column is nullable and
  editable. Frontend `0257fe4`: the table gets sortable invoice-date and payment-date columns
  with `invoiceDate` as the default sort, plus an `Empfänger/Zahler` column. It hides
  `created_at`. The create and edit dialogs show all fields, including the bank account.
  Decisions: use the label `Empfänger/Zahler`, sort by invoice date by default, keep all
  metadata and the account editable, and add the extra fields `Belegnummer`, payment method and
  category.

**Done and committed (continued):**
- **#6** (`365b63d`): the datepicker host is a block with `min-width:0`, so narrow dialogs no
  longer overflow or scroll.
- **#15** (`4dd511a`): the dialog body got vertical padding (`--space-1` = 4px), so the edge no
  longer clips the focus ring. Chosen approach: pad the clipping containers. The user must check
  this by eye.
- **#17** (`1d93555`): the formly checkbox uses `align-items:center` and `margin:0`, which makes
  the vertical margins equal. The user must check this by eye.
- **#16** (`eb96123`): the expanded roles row uses a `.roles__nameEdit` grid with even Key, DE
  and EN columns and Save on its own footer line. The cramped gaps are now even. The user
  approved layout option B.

**Done and committed (continued):**
- **#9** (`961a163`): the data-table empty state is centered on mobile, which matches the
  applications list. The desktop view already matched.
- **#14** (`8f4e695`): mobile dialog. The body locks scrolling. A grabber allows flick-to-close:
  drag the grabber or the header with a velocity above 0.5 or a distance above 120px. The dialog
  sits flush with the bottom safe area. Tap-outside already worked.

**Done and committed (continued):**
- **#10** (`76ee1b0`): the meetings timeline got a mask fade at the top and the bottom plus a
  slim scrollbar.
- **#7** (`424a855`): the substitute pool now sees the meeting timeline of its Gremium. The
  backend `_visible_gremium_ids` adds the pool Gremien, `whoami` returns `in_substitute_pool`,
  and the frontend gates on it. The live channel still needs a membership or a delegation.
  Decision: give timeline visibility only, not live access.

**#5-4 audit done** ([[be-fe-field-gaps-2026-06-13]]). The user picked all 3 gap categories, so
work follows on each of them:
- DONE (`be5a898`): the agenda and the assignable list show the application state labels.
- DONE (`8c30f4c`): the open-live-vote dialog now sets `majorityRule`, `eligibleCount` and
  `quorumPercent`. The backend already read them.
- TODO: small scattered display gaps. Expense `actor`, `accountName` and `transferId` columns,
  `MeetingOut.closedAt`, `AttendanceOut.source`, `VoteOut.opensAt` and `VoteOut.eligibleGroup`,
  `VoteConfig.tieBreak` and `VoteConfig.abstainCountsQuorum`, `ProtocolOut.sentAt`,
  `TimelineEventOut.actor`, `FormVersionOut.version`.
- DONE (`dc370c6`): **GroupMapping page** at /admin/group-mappings behind `admin.roles`. It has a
  table, an add and edit dialog, and delete. The missing backend DELETE endpoint is now there.
  The frontend is complete.
- DONE: `be5a898` agenda state labels. `8c30f4c` open-vote majority and quorum config.
- DONE (`1a7f894`): **MailTemplate editor** at /admin/mail-templates behind
  `admin.notifications`. It shows a list, DE and EN tabs for subject, body and HTML, a
  placeholder reference and a live preview. The service CRUD existed already. A
  `templates_router` (GET, POST, PATCH and /preview) was added in main. The frontend is
  complete. NOTE: the preview renders the SAVED template, because the backend renders by id. It
  does not render unsaved edits.
- DONE (`7b73270`): **comparisonOffers config** in the form editor, with `required` and
  `minCount`. It keeps `threshold` and `as`. Frontend only, because the backend already accepted
  it.
- **All 3 missing #5-4 features are done.** Only the small display gaps remain, see
  [[be-fe-field-gaps-2026-06-13]].

**Still open:** #8 ([[budget-import-zugferd]]), #11 (wiki), #12 (security audit, a candidate for
the ultracode workflow), #13 (MCP, needs a scope), #18 (adapters, open design question).

**Session total: about 20 items and 21 commits on branch fix/gremium-member-perms. The branch
sits off main and is NOT pushed. Both suites are green: backend 1443, frontend 582.** The deploy
still needs **migration 0023** and a stack rebuild. The user must check #14, #15, #17 and #10 on
a phone by eye. One ruff nit is pre-existing and not from this session: the import sort at
protocol/service.py:26.
**#5-4 display gaps DONE (the meaningful ones):** `0f09f55` expense account, transfer and
booked-by. `94ed2fe` timeline actor and form version. `ee0b0b2` attendance source. The marginal
ones are deferred: vote `eligibleGroup` and `opensAt`, `VoteConfig.tieBreak` and
`VoteConfig.abstainCountsQuorum`, `MeetingOut.closedAt` (missing on the frontend type),
`StateOut.kind`, `ApplicationOut.lang`.
**#12 security audit DONE:** the findings and their status stay private and are not in this repo.
The code fixes landed on the relevant branches.
**Build fix `35bacf9`:** ng build budget, see [[ng-build-budgets]].

**Still open backlog. Every item needs a scope, a design, research, or an external repo:** #8
ZUGFeRD ([[budget-import-zugferd]], library research and drag-drop), #11 wiki (external
…wiki.git), #13 MCP (needs a scope from the user), #18 adapters (design question: which ones).

Stack: backend FastAPI, SQLAlchemy and Alembic in `backend/app/modules/*`. Frontend Angular in
`frontend/src/app` (pages/, features/). MCP in Python. The expense model is `BudgetExpense` at
`backend/app/modules/budget/tree_models.py:160`.
