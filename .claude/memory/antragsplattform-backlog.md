---
name: antragsplattform-backlog
description: Outstanding feature backlog for the antragsplattform (StuPa motions platform) as of 2026-06-08
metadata: 
  node_type: memory
  type: project
---

Branch `feat/admin-ux-flow-editor-fixes` (PR #126).

DONE 2026-06-09 (Meetings overhaul, pushed): **forced gremium-roles** vorstand/schriftfuehrung/member in EVERY gremium (seed on create + lazy on list + migration 0031 backfill; delete-guard; GremiumRoleOut.forced flag; FE roles page hides delete). **Meeting control gated on Sitzungsleitung** — PATCH /meetings authoritative requires vorstand/schriftfuehrung gremium-role (or admin); MeetingOut.canControl drives FE hide of control+planning cards (LEAD_ROLE_KEYS in admin/gremium_roles.py; MeetingService.can_control). **Attendance** (#55/#56): meeting_attendance table (migration 0032) + AttendanceService (roster=current gremium_membership; present/excused/absent; source self/lead); GET /meetings/{id}/attendance, PUT …/attendance/me (member self), PUT …/attendance/{principalId} (lead); FE attendance card. **Protocol autosave** (#56): debounced PATCH, no manual save button, Saving/Saved status. **Markdown preview** upgraded (no dep): pipe tables/links/ordered lists/HR in meetings.util.renderMarkdown. **Altcha fix**: widget was a stub emitting 'altcha-stub-solution' → real PoW solver (GET /altcha/challenge + Web-Crypto SHA-256 brute force + base64); 404→unavailable; was the cause of submit 422/"Altcha verification failed". Also: gremium-role dialog input styling, flow-editor edges start at branch dot + bezier, breadcrumbs single-source budget-style no-Dashboard-prefix, borderless user-icon account menu, members route → /admin/gremien/:id/members.

DONE this session: #9 budget Kostenstellen tree (FE on existing /api/budgets backend), #11 per-Gremium members (Gremien admin), #12 Users Nextcloud-table + roles split (/admin/roles) + admin role locked. Plus the earlier UX/flow rounds.

Also DONE: #14 per-Gremium vote-delegation flag, #17 Budget-Statistik drilldown, #18 Gremien rework, #22 budget↔gremium decoupled + FY-inside + per-row limit dialog.

Also DONE: #20 (then templates REMOVED again by request), shared DataTableComponent (#26 started, Gremien migrated), Abstimmungen nav tab removed (voting via Sitzungen), budget-statistik decluttered.

BIG OPEN REDESIGNS (need design/scoping before building):
- **#28 GLOBAL flow redesign** — one global flow with global states (not per Antragstyp); special VOTING states ("Gremium X must vote") replacing the voteResult auto-transition; states where "role X in gremium Y accepts/rejects"; decision nodes routing by amount/type/applicant-role; clearly separate gremium-roles vs global-roles. Large BE+FE. Supersedes #23. Design decided: drop all existing entries (pre-alpha hard cutover); two fixed outputs per vote/approval state; backend model+engine FIRST. BE model already started (State.kind CHECK/vote/approval/decision + config JSONB, Transition.branch, FlowVersion.application_type_id nullable + global/per-type partial unique indexes, config_schemas validation, migration 0023).
- **#42 Gremium-roles model** (ties into #28's gremium-vs-global split): within a Gremium a user has EXACTLY ONE role at a time; gremium-roles are a SEPARATE role set from global roles, configured on their own page under admin/gremien/...; assigning uses a dropdown of gremium-roles; gremium-membership is TIME-BOUNDED (valid_from/valid_until = Amtszeit), global roles are PERMANENT/INDEFINITE (no expiry — drop the validity window from the global-role assign UI on admin/users); multiple NON-OVERLAPPING memberships of the same gremium allowed (consecutive terms), overlapping forbidden. Current bug: user can hold multiple simultaneous roles in one gremium — wrong.
- **#46 Granular permissions everywhere** — audit every admin/action endpoint + UI control, gate each behind a SPECIFIC granular permission (not broad admin). BE checks authoritative; FE hides/disables. Big cross-cutting.
- **#45 Audit-Log tab** — admin view of the append-only audit trail, gated by a dedicated permission (e.g. audit.read).
- **#13 Form-Builder** like Nextcloud Forms (+ admin "Anträge" tile/list, add via dialog).
- **#26 finish** migrating all tables to the shared DataTable; everything as shared components.
- **#29 Seiteneinstellungen** — replace versioned Branding&Texte with direct-edit current values.

DONE 2026-06-08 (full-auto run, all on branch + pushed): row-height uniform (DataTable fixed 3rem), #40 self-admin-remove guard, #41 delete Gremium, #43 equal admin tiles, #44 self-suspend guard + UI, #47 icon-button tooltips (shared Button title→ariaLabel), #48 role-tags scroll, #49 budget Anträge stacked, #50-fix role-tag × inside badge, #15 admin=all-rights (Principal.has), #51 header account popout (logout only there), #52 theme toggle neutral in dark, #53 lang chevron mask, #54 dashboard gremium badges, #39 webhooks+notifications reworked to header+DataTable+dialog, #45 Audit-Log page (GET /admin/audit, audit.read). #28 ENGINE: flow/routing.py decision DSL + FlowService.route_decision/fire_branch/branch_transition/submit_approval + _has_gremium_role, POST /applications/{id}/approval, vote close fires pass/fail branch, app creation uses active global flow.

#60 FLOW-UI DONE (2026-06-08, pushed): approval Accept/Reject on application detail (POST /approval; StateOut.kind added BE); flow editor authors state.kind (normal/vote/approval/decision) + per-kind config (vote→gremium; approval→role-source toggle: gremium-role OR global role; decision→rules builder field/op/value→target + else) + transition branch (pass/fail/accept/reject); editor is GLOBAL (no type dropdown — loads/saves the active global flow via GET/POST /admin/flow-versions/global); client-side kind validation mirrors BE so save shows the specific error (e.g. "vote needs pass+fail"). BE: approval may omit gremiumId → global-role decision. Graph serializer carries kind/config/branch.

#62 PER-GREMIUM ROLES (2026-06-08, pushed): gremium_role now has gremium_id FK (migration 0027, Unique(gremium_id,key)); endpoints /admin/gremien/{id}/roles; FE roles page is a SUBPAGE at admin/gremien/:id/roles (linked from gremien table edit-icon). REMAINING for #62: the per-gremium **members** view still uses global RoleAssignment — needs a gremium-membership assignment UI (role dropdown of that gremium's roles + Amtszeit + list/delete) using /admin/gremien/{id}/memberships.

DONE 2026-06-08 batch 3 (pushed): #61 member role auto-granted every login + unremovable; #62 FINISHED — gremium members subpage assigns gremium_membership (this gremium's roles + Amtszeit, overlap→409), FlowService._has_gremium_role now uses gremium_membership/gremium_role; #63 route-driven breadcrumbs in shell; #64 Aufgaben tab (GET /applications/tasks → vote/approval apps actionable by principal; FE nav+table→detail); #65 table top spacing; #66 flow builder polish (vote/approval nodes show one labelled out-dot per branch pass/fail|accept/reject, drag presets branch; kind label on nodes; removed intro blurb). NEXT MAJOR (user-queued): #13 NC-Forms-style form builder (current form-builder is a flat field-list, needs forms-list+add-dialog → per-form NC-style editor: title+md desc, +Add-question type menu, drag-reorder, View/Edit toggle).

#13 FORM-BUILDER DONE (2026-06-09, pushed, commit 2731ad3): NC-Forms rework. `admin/forms` now a FormsListComponent (DataTable of application types + add-via-dialog: title DE/EN, Gremium, hasBudget; key auto-slug → navigate to editor). `admin/forms/:id` = FormEditorComponent (NC-style): title + multilingual MD description, View/Edit toggle, question cards (type menu via app-select friendly labels, label/help DE+EN, required, options, advanced ⋯ = key/PII/promote/validation/JsonLogic, drag + arrow reorder, duplicate/delete), "+ Frage hinzufügen" type menu. Reuses form-field.util. OLD form-builder component DELETED. admin-home overview edit-icon deep-links to editor. BE: form_version.description_i18n (migration 0028); FormVersionCreate/Out carry description; new admin GET /admin/application-types/{id}/form-versions/latest → FormDraftOut (raw fields + description) for editing (service.get_form_draft). admin-api: listApplicationTypesFull/createApplicationType/updateApplicationType/getFormDraft + createFormVersion(typeId,fields,description); mock store MOCK_APP_TYPES + MOCK_FORM_DRAFTS. i18n DE+EN: admin.forms.* + admin.form.type.* + admin.common.cancel/delete. Specs: forms-list + form-editor. NOTE: MD description rendered as pre-wrap plaintext (no MD lib) — safe, no HTML inject; upgrade later. BE create-application-type/PATCH endpoints already existed.

#16 EN-EDITING DONE (2026-06-09, pushed e82a0e6): branding/site-config editor now has EN inputs for copyright, legal-link labels, footer-link labels, all 4 freetexts (login/welcome/support/emailFooter) — two-col DE/EN (.br__bi). Composite aria-labels "<field> (DE)/(EN)". Spec added.

#21 ALREADY DONE: roles page has add-dialog (createRole), delete (deleteRole), permission edit (saveRolePermissions). Backlog stale — nothing to do.

#25 BUDGET-EXPENSE DONE — BACKEND ONLY (2026-06-09, pushed dc3e81f): standalone expense vs a Kostenstelle+HHJ, no Antrag. New budget_expense table (budget_id, fiscal_year_id, amount>0 EUR, description, actor) migration 0030. POST/GET /api/budgets/{id}/expenses (create P(budget.manage), list P(budget.view), node+subtree, ?fiscalYear filter) + DELETE /api/budget-expenses/{id}. Expenses fold into get_tree committed_rows → roll up via path_key prefix exactly like approved apps (consumed↑, available↓). FY from request or single active FY of top (ambiguous→422). Tests: router wiring+RBAC, _resolve_expense_fiscal_year branches, rollup. REMAINING #25: FE UI to book/list expenses on the budget tree (budget-tree.component) — NOT done, deferred to avoid colliding with other agent in FE.

NOTE 2026-06-09: a SECOND agent works FE (breadcrumbs/forms/form-editor, then meetings/attendance + gremium forced-roles). Hands off form-editor/meetings/attendance/shell. They `git add -A` commit — may sweep up your uncommitted shared-file edits (happened with models.ts/mappers.ts). Stage your own files explicitly + commit promptly.

#24 LOGGED-IN APPLY DONE (2026-06-09, pushed f3f131c BE + 078fc19 FE): user reframed #24 = the apply flow from the dashboard for logged-in users, NOT admin-on-behalf. (1) Logged-in users skip Altcha; (2) email/name derived from account. BE: new require_altcha_unless_authenticated dep (skips altcha when principal present; anonymous still checked) on POST /applications; ApplicationCreate.applicant_email now OPTIONAL; router derives email=payload or principal.email, name=payload or principal.display_name, actor=principal.sub else "applicant"; anonymous-missing-email→422. ApplicationsService.create gained actor param (StatusEvent + SubmissionVersion.changed_by). Dropped the earlier admin-on-behalf /admin/applications endpoint idea (redundant — logged-in Verwalter passes applicantEmail). FE: apply-wizard injects AuthService, drops contact step + altcha widget when loggedIn (sectionBase 1 vs 2), sends null email/altcha (backend derives), review shows account email; models.ts ApplicationCreateBody/NewApplication applicantEmail+altcha now nullable; mapper ?? null. Tests BE+FE.

#13 EXTRA DONE (2026-06-09, pushed): **budget toggle + metric dropdown** (6b8adb1): "Mit Budget" (has_budget) toggle now in the form EDITOR meta (persists via PATCH /admin/application-types alongside title); "Ziel-Kennzahl" (promoteTarget) is now a DROPDOWN of valid metrics (only `amount` wired) — PROMOTE_TARGETS in form-field.util.ts; enabling isPromoted defaults target to amount.
**positions field type + system title** (BE b48bc11, FE dcc389f): every application MUST have a title — effective_form auto-prepends a required system `title` text field to the main section unless the form already defines one (validation.system_title_field/SYSTEM_TITLE_KEY). New `positions` FieldType (cost positions): value = list of {label, offers:[{label,value,preferred}]}; validation _validate_positions (>=minPositions[def 1], each >=minOffers[def 3] offers, exactly one preferred, values finite >0); implicitly promotes Σ preferred-offer-values into `amount` (extract_promoted positions branch, no isPromoted flag). FieldValidation gains minOffers/minPositions. FE: form-editor positions config block (minOffers/minPositions); custom Formly type FormlyPositionsType (shared/formly/types) — repeating Position→Offers editor, preferred radio, live total, validity mirrored to formControl via revalidate (ngOnInit too so empty=invalid); registered in formly.providers; formly-mapper maps positions→'positions' + passes minOffers/minPositions props; apply-wizard review shows total. NOTE: only `amount` promote target is consumed downstream — other targets extracted but unused. Did NOT reserve `title` key (too much fixture churn; effective_form inject-if-absent suffices).
APPLICATION DETAIL FIX (a5455b9): detail page rendered raw data keys + blank transition pills. Now: data uses effective-form field labels (api.effectiveForm(typeId), fetched alongside app, degrades to raw keys) + type-formatted values (select/multiselect option labels, currency localised, checkbox yes/no, positions count+total); system `title` omitted (it's the heading); empty-label transitions fall back to generic "Weiter"/"Advance". refresh() does NOT refetch the static form (only loadApplication does) — spec flushAll has form=true param.

SESSION 2026-06-09 LATE (many commits pushed, branch feat/admin-ux-flow-editor-fixes):
- positions field type (BE config_schemas+validation; FE custom Formly type shared/formly/types/formly-positions); system `title` field auto-injected by effective_form; positions auto-promotes Σ preferred→amount.
- form builder: key at top of question card; budget toggle + metric dropdown in editor; type-aware advanced panel (no promote on non-numeric → fixes 422 "promoted must be numeric"); positions config minOffers/minPositions.
- application detail: typed/labelled data via effective form; removed manual "Status change" transitions card (flow-driven); email row; INLINE EDIT (Formly) + DELETE gated on canEdit; positions total.
- #24 creator access: application.created_by (migration 0034) set on logged-in create; require_app_read/edit fall back to creator; DELETE /applications/{id}; ApplicationOut.canEdit.
- title column in Applications+Tasks lists (BE ApplicationListItem.title from data['title']).
- tasks: inline accept/reject (approval) / Open (vote).
- attachments persist: NEW GET /applications/{id}/attachments + FE listAttachments + panel hydrates on init (data was always in DB+MinIO; just no list endpoint).
- removed unimplemented "My committees" tab.
- #20 applications table: rounded border + filter POPOUT (button+funnel icon+active-count badge) + amount-range + date-range filters + sortable amount/created headers. BE list gained amountMin/Max, createdFrom/To, sort, order.
REMAINING (other agent's contended files — coordinate): #17 flow editor (branch-filter by node kind, roleKey reactivity shows only after reselect, decision node + decision-question field, NEW applicant-submit node kind, reject free-text reason); #22 budget ((auto-)assign application to budget — BE assign-budget endpoint EXISTS, FE missing; fiscal-year year-only + start-on-budget; rename "Top-Budget"→"Budget"); #20 leftovers (cost-position subtree filter, all-states list). GOTCHA: type=number ngModel emits a NUMBER not string (broke v.trim() in active-filter count → use String()).

GOTCHA — LOCAL .env ENABLES ALTCHA: backend/.env sets ALTCHA_HMAC_SECRET → altcha_enabled True locally → ~6-8 altcha-"disabled"-expecting tests (test_antiabuse/test_applications_router/test_auth_router) FAIL locally but PASS in CI (no .env). conftest assumes altcha OFF. To run those locally CI-style: `mv backend/.env aside` then pytest. Don't chase these as bugs.

#42 PROGRESS (2026-06-08): backend DONE — gremium_role + gremium_membership tables (migration 0026), GremiumRoleService with pure intervals_overlap (one active role per principal+gremium; consecutive non-overlapping terms OK), admin endpoints /admin/gremium-roles + /admin/gremien/{id}/memberships + /admin/gremium-memberships/{id}. FE catalogue page admin/gremium-roles DONE (CRUD). REMAINING: per-gremium membership-assignment UI on the gremium-members subpage (assign a gremium-role + Amtszeit window via dropdown, list/delete memberships) — currently that page still uses global RoleAssignment; needs switch to the new gremium_membership API.

NEW BIG BACKLOG (2026-06-08, not started): #50 Anträge table (rounded border, lazy infinite-scroll, SAME table reused under Budget w/ filter, rich filter pop-out: Kostenstelle tree incl sub, amount-range slider; active-filter hint badge). #55 attendance confirm/excuse per planned meeting (per-gremium lead-time window). #56 protocol editor: markdown library + live server save + actual-attendance picker. #57 attendance stats per member over Amtszeit (excused/unexcused/present). #58 protocol native TOP list (TOP = Antrag or freetext). #59 Excel export budgets+Anträge (filter-aware). #46 granular permissions everywhere. #42 gremium-roles model (see above).

SMALLER OPEN:
- #19 add-via-dialog everywhere; #21 configurable roles (BE has POST/PATCH /admin/roles); #24 manual Antrag creation; #25 standalone budget expenses; #27 meetings dialog+shared table; #15 admin=all-rights BE; #16 EN editing everywhere.

STILL OPEN:
- **#20** flow editor: drop Expert mode, savable templates, more help text. See [[ui-patterns-and-backlog2]].
- **#21** configurable roles (create/edit/delete) — backend already has POST/PATCH /admin/roles; add FE role CRUD.
- **#23** flow rule: amount-threshold → specific gremium vote (guard + gremium-scoped action).
- **#24** manual Antrag creation as a Verwalter (permission + UI).
- **#25** budget expenses independent of an Antrag (standalone expense against a budget node + FY).
- **#10 Meeting agenda** — assign Anträge to Sitzungen (Image 12). Backend has only single `activeApplicationId` + `POST /applications/{id}/votes`; no agenda model yet.
- **#13 Form-Builder** — rework ~1:1 like Nextcloud Forms (large FE rewrite). See [[nextcloud-parity-ui]].
- **#14** vote-delegation → per-Gremium setting (per-user checkbox already removed; needs Gremium flag + UI). See [[admin-domain-rules]].
- **#15** enforce admin=all-permissions in the BACKEND (FE already locks it). See [[admin-domain-rules]].
- **#16** allow EN editing for all i18n values, not just DE (branding/gremium/form labels). See [[admin-domain-rules]].

SESSION 2026-06-09 NIGHT (pushed, branch feat/admin-ux-flow-editor-fixes):
- tasks: iconic accept(green check)/reject(red cross) for approval rows, Open button removed (row-click opens detail); added `success` Button variant + `check` Icon (5d44ce5).
- admin breadcrumbs: every admin sub-route carries parent:['admin'] (nested → ['admin','admin/forms'] etc.) so crumbs link back to /admin (5d44ce5).
- gremium-roles edit-button alignment (Image#4): forced roles now show a DISABLED delete button (same size) instead of a wide "Pflicht" label → edit pencil stays aligned (5d44ce5).
- flow editor: ONE output dot per distinct guard on normal nodes (+ catch-all dot for new edges); edges start at their guard's dot; state inspector lists guards in eval order with ↑/↓ that restamp Transition.order (first-match wins). Backend already: admin/service.py order=enumerate(transitions) fallback; flow/service order_by(Transition.order) (845b3d2).
- positions field: offer-remove disabled at minOffers; Wert input format-on-blur (parses 1.234,56 & 1234.56); INLINE errors (invalid inputs red + per-card concise message instead of bottom notice) (96d05be + earlier).
- application detail: full cost-position breakdown (each position + its offers, preferred highlighted, positions total) read-only; edit-form Formly fields spaced vertically (Image#5 spacing) (96d05be).

MULTI-STEP + DEADLINES DONE 2026-06-09 NIGHT (pushed 50e8b29 + 2e76ba5):
- **MULTI-STEP FORMS** (50e8b29): new `section` FieldType = divider marker (label only). Backend effective_form() splits main fields at markers into titled FormSections (title injected into FIRST section AFTER split so a leading marker doesn't make a title-only step); markers stripped from fields + skipped in validate_answers. FormSection gained `label`; service uses s.label or SECTION_LABELS fallback. FE: form-editor '+ Abschnitt' (auto-key section_N, title-only card + preview divider); formly-mapper filters section out; models.ts/form-field.util FIELD_TYPES += section. Apply wizard already steps per eff.section → authored sections become steps free. Tests BE (test_forms_validation split/leading/skip) + FE (mapper drops markers).
SESSION 2026-06-09 (continuation, pushed):
- **TASKS mock fix** (0d473b0): mock-api.interceptor had NO `/applications/tasks` handler → the single-app catch-all `/\/applications\/[^/]+$/` returned one object not an array → Aufgaben page broken under USE_MOCK_API. Added MOCK_TASKS (approval + vote rows the principal may act on) + POST `/applications/{id}/approval` handler that splices the decided row so it vanishes on reload.
- **DEADLINES ENFORCEMENT WIRING DONE** (a874987): the deferred follow-up is now built. State config carries `deadlinePolicyKey`; FlowService.schedule_state_deadline(app, state) resolves the policy (resolve_due_at, created_at/updated_at refs) + creates a Deadline with action_on_pass→the state's `deadlinePassed`-guarded transition (found via module fn `_guard_fires_on_deadline`, recursive and/or/not scan); prior unfired flow_deadline rows cleared first. Called from applications.service.create (initial state, after commit+refresh) AND flow.service.fire (to_state, after commit+refresh). T-44 cron fires it. Flow-editor: state inspector has a Deadline-policy `app-select` (loads listDeadlinePolicies) + setStateDeadlinePolicy + hint; config passes through normalizeFlowGraph wholesale (no migration). i18n admin.flow.cfgDeadline/cfgDeadlinePh/cfgDeadlineHint de+en. Tests: guard scanner + schedule-creates-row + no-op-without-key (test_deadline_policies, 10 pass). flow-editor spec mock got listDeadlinePolicies. NOTE: 3 local altcha-.env failures (test_antiabuse/test_applications_router) are the known GOTCHA, pass CI-style.

- **DEADLINE-POLICY REGISTRY** (2e76ba5): deadline_policy table (migration 0037) — key(unique)+label+kind('absolute'|'relative_submitted'|'relative_changed')+absolute_at+offset_days. resolve_due_at(policy, submitted_at, changed_at) pure helper. Admin CRUD /admin/deadline-policies (admin.config) in deadlines/router.py (DeadlinePolicyService); registered main.py + models.py. FE admin 'Fristen' page (pages/admin/deadlines, DataTable+dialog, kind-aware date/offset), route admin/deadlines (parent ['admin']) + admin-home tile + i18n admin.deadlines.*. Tests BE (resolve + router) + FE (component spec). REMAINING follow-up (NOT done): ENFORCEMENT wiring — a flow state config references a policy key → on entering the state, resolve_due_at + DeadlineService.create a Deadline row (action_on_pass=transition) so the existing T-44 cron fires deadlinePassed; + flow-editor UI to pick a policy key on a node. The guard op `deadlinePassed` (shared/guards.py, ctx.deadline_passed bool) + cron substrate already exist.

OLD QUEUE (now built above):
- **MULTI-STEP FORMS** (decision: "section field = step"). Apply wizard ALREADY steps per eff.section; builder is FLAT (one fields[] list) and backend effective_form lumps all type-fields into ONE "main" section. PLAN: add `section` FieldType = a divider/marker carrying an I18n label; backend effective_form() splits main_fields at each `section` marker into multiple FormSection(label=marker.label) (strip markers from fields); FormSection dataclass needs `label`; service uses s.label (fallback SECTION_LABELS for main/budget); validate_answers + extract_promoted must SKIP type=='section'; FieldType union+'section' in config_schemas.py AND FE models; form-editor +Add menu gets "Abschnitt/Section" (auto-key section_N, only label editable, render as divider). Apply/detail already consume eff.sections.fields (markers stripped server-side) → multi-step free. Needs BE+FE tests.
- **FLOW DEADLINES = NAMED POLICIES** (decision: "Named deadline policies"). New admin "Fristen" page + backend module: a DeadlinePolicy has a key + kind = absolute (a date, set per semester, editable anytime WITHOUT touching the flow) OR relative (submitted + X days / lastChange + X days). Flow nodes/types reference a policy by key. Enforcement reuses existing `deadlinePassed` guard op (flow). PLAN: backend deadline_policy table + CRUD (admin.*), resolve(policy, app) → concrete datetime (absolute value or app.created_at/updated_at + X days); flow guard `deadlinePassed` reads referenced policy; admin Fristen CRUD page (per-semester date edit, relative offsets); flow editor lets a node reference a policy key. Big BE+FE+migration.

Stack: Angular 21 (signals, standalone, Jest + testing-library, `npm test`=jest; needs `npm install` — workspace ships partial node_modules) frontend; FastAPI + arq worker + SQLAlchemy/Alembic backend (venv lacks pytest by default). i18n is a typed `de`/`en` catalog in `core/i18n/translations.ts` (add keys to both).
GOTCHA 2026-06-09 night: other agent's IN-PROGRESS markdown-editor/ (untracked) + meetings.component.ts (savingTop) break `ng build` in the working tree — NOT your files; verify your changes via isolated `jest <area>` and stage only your own files.

SESSION 2026-06-09 (TASKS.md run, branch feat/admin-ux-flow-editor-fixes):
- TASKS.md = NEW 11-task list (admin UX/platform), separate numbering from this backlog. Working through it.
- TASK#1 DONE: admin-home restyled — icon-left tiles (added icons building/euro/form/flow/palette/webhook/bell/audit/clock/export to IconComponent), removed active-forms table, shortened users/gremien descs. Spec rewritten.
- TASK#2 DONE: Excel export. New perms budget.export(admin/finance/manager)+application.export(admin/manager) in PERMISSION_CATALOGUE + admin.mock MOCK_PERMISSIONS. Migration 0043_export_permissions. ALSO FIXED multi-head: two 0041 (budget_color_states + agenda_freetext_fix both off 0040) → linearized budget_color_states→0042_agenda_freetext_fix→0043. openpyxl dep added + installed in venv. BE: GET /api/budget/export.xlsx (tree_router, node/fiscalYear/gremium filters, _find_subtree) + /api/applications/export.xlsx (router, reuses list_applications limit 100k). xlsx builder app/shared/xlsx.py (lazy openpyxl). service helpers: BudgetTreeService.fiscal_year_label_map, ApplicationsService.name_maps. FE: exportXlsx on BudgetTreeApi, exportApplicationsXlsx on ApiClient, shared downloadBlob util, gated buttons on budget-dashboard + applications-list. i18n budget.export/applications.list.export de+en. Tests: 46 pass (test_budget_tree_router + test_applications_router).
- GOTCHA: .env MUST stay present for create_app (settings need database_url etc.); only move aside for altcha-sensitive pytest runs.
