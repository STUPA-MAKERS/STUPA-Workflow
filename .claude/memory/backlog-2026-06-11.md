---
name: backlog-2026-06-11
description: "DONE backlog 2026-06-11 — audit-log fixes (#1–#3), mail/notification rework (#4 series), PWA (#5), granular perms (#6); branch feat/backlog-audit-mail-pwa-perms, not merged"
metadata: 
  node_type: memory
  type: project
---

**STATUS: ALL DONE + MERGED TO LOCAL MAIN (2026-06-11, merge commit 6c73b47).** Backend 1405 + frontend 569 tests green, ruff/eslint/tsc clean. NOT pushed to origin (main ahead of origin incl. delegation-rework 13d5a89).

Also done: stale-task reminder worker (#task-reminder) — admin-configurable via new page `/admin/notifications` (new permission `admin.notifications`, granted to admin.site holders by migration): enabled flag, remind-after days, repeat-every days (0=once per state stay); hourly cron `process_task_reminders`, dedupe via `task_reminder_log`.

Merge with main's delegation-rework: delegation mails re-attached to meeting-bound `meeting_delegation` model (AutoMailer.delegation_changed + meeting_delegation_mail_info); **migrations renumbered**: 0015_drop_pii_anonymization, 0016_notification_preferences, 0017_granular_permissions, 0018_notification_settings (main's 0014_meeting_delegations dazwischen).

Nachträge auf main (2026-06-11/12): PWA-SW-Fix (`!/api/**` navigationUrls), Mail-Linkify, Notification-Settings-Styling, Dashboard `mine=true`-Filter, **Vertretungs-Stimme = getrennte Abgabe** (`asDelegation`, Ballot unter Delegator-Sub, Cast-Gate auth-only + Service-Autorisierung), Vertretungs-Karte gestylt, Docker pip-Cache (Dep-Layer + BuildKit-Cache-Mount), **Typ-Flows komplett entfernt** (Migration 0019, 0006 column-guarded, MCP-Tool weg), Flow-Editor Sugiyama-light Auto-Layout. Docker läuft jetzt lokal → Integration-Tests laufen echt (1407 passed).

Open follow-ups:
- `git push` ausstehend.
- Deploy needs migrations **0014–0018** + stack rebuild (worker too — neuer Cron).
- DB-overridable mail template keys: comment_applicant, comment_team, task_new, task_reminder, meeting_created, role_assigned, role_revoked, delegation_granted, delegation_revoked.

Backlog tracked 2026-06-11 (task tool #1–#9, user numbering #1–#6):

- **#1** Export actions not logged in audit log — add logging.
- **#2** Audit-log UI rework: human-readable, compact, for every event type.
- **#3** Audit log has artifacts of long-dropped features **PII** and **Anonymization** — drop all remaining artifacts (code/schema/UI).
- **#4** Mail rework: HTML, prettier, more context on trigger.
  - **#4-1** Mail applicant on new comment; mail all role-holders who can act on current state when applicant comments.
  - **#4-2** Notification settings page reachable from username dropdown in header; users configure which notifications they receive.
  - **#4-3** More notification types — propose options to user first.
- **#5** PWA manifest so site installable as app.
- **#6** More granular global permissions, settable in role-config admin page.

**Decisions (asked 2026-06-11):**
- #2: Nextcloud-style activity feed — grouped by day, icon per action category, one human-readable sentence (actor + action + target link), expandable details row.
- #3: FULL anonymization feature removal — not just audit enum artifacts. Delete `anonymize()` (applications/service.py:819), `anonymized_at` column (migration), `isPII` form-field flag, recipient-resolver skip, frontend/i18n traces.
- #4: HTML mails = code base-layout (logo, colors, footer with "why you got this" + settings link) + DB MailTemplate inner content per type/language.
- #4-2: prefs all-on by default, opt-out per type; essential mails (magic-link) not disableable. Table principal × type, page /account/notifications, link in username dropdown.
- #4-3: ALL four groups approved: own-application lifecycle (every status change), task notifications (state actionable for role + stale reminders), meeting & vote events (agenda published, vote opened/closed), role & delegation events (assigned/revoked).
- #5: FULL @angular/pwa — service worker + manifest + icons, not manifest-only.
- #6: all four splits approved: admin.config → admin.site/admin.gremien/admin.types; budget.manage → budget.structure/budget.book; meeting.manage → meeting.manage/protocol.finalize; audit.read → audit.read/audit.verify. Migration must remap existing role_permission rows.

Facts found: EXPORT audit action exists but never recorded; 3 export endpoints (applications/router.py:242, budget/tree_router.py:99,287). Mail infra has unused HTML support (MailMessage.html, body_html_i18n). `comment_added` event exists, no handler. No user-pref table. Account pages at /account/* (grants, consent); dropdown in layout/shell.component.html:36-68.

User insists: ask via question tool when unclear, consult before design decisions, no assumptions. See [[work-autonomously]], [[track-side-requests]], [[admin-domain-rules]].
