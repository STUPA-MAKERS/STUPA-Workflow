---
name: backlog-2026-06-11
description: "DONE backlog 2026-06-11 — audit-log fixes (#1–#3), mail/notification rework (#4 series), PWA (#5), granular perms (#6), branch feat/backlog-audit-mail-pwa-perms, not merged"
metadata: 
  node_type: memory
  type: project
---

**STATUS: ALL DONE + MERGED TO LOCAL MAIN (2026-06-11, merge commit 6c73b47).** Backend 1405 +
frontend 569 tests green, ruff/eslint/tsc clean. NOT pushed to origin (main is ahead of origin
and includes delegation-rework 13d5a89).

Also done: the stale-task reminder worker (#task-reminder). An admin configures it on the new
page `/admin/notifications` (new permission `admin.notifications`, granted to admin.site holders
by a migration): enabled flag, remind-after days, repeat-every days (0 = once per state stay). An
hourly cron `process_task_reminders` runs it and dedupes through `task_reminder_log`.

Merge with the delegation-rework on main: delegation mails now hang on the meeting-bound
`meeting_delegation` model (AutoMailer.delegation_changed + meeting_delegation_mail_info).
**Migrations renumbered**: 0015_drop_pii_anonymization, 0016_notification_preferences,
0017_granular_permissions, 0018_notification_settings, with main's 0014_meeting_delegations in
between.

Follow-ups on main (2026-06-11/12): PWA service-worker fix (`!/api/**` navigationUrls), mail
linkify, notification-settings styling, dashboard `mine=true` filter, **a delegated vote is a
separate ballot** (`asDelegation`, the ballot sits under the sub of the delegator, the cast gate
is auth-only plus service-side authorization), delegation card styled, Docker pip cache
(dependency layer + BuildKit cache mount), **per-type flows removed completely** (migration 0019,
0006 column-guarded, MCP tool dropped), flow-editor Sugiyama-light auto-layout. Docker now runs
locally, so the integration tests run for real (1407 passed).

Open follow-ups:
- `git push` is still pending.
- The deploy needs migrations **0014–0018** plus a stack rebuild, the worker too, because of the
  new cron.
- DB-overridable mail template keys: comment_applicant, comment_team, task_new, task_reminder,
  meeting_created, role_assigned, role_revoked, delegation_granted, delegation_revoked.

Backlog tracked 2026-06-11 (task tool #1–#9, user numbering #1–#6):

- **#1** The audit log does not record export actions. Add the logging.
- **#2** Audit-log UI rework: human-readable, compact, for every event type.
- **#3** The audit log has artifacts of the long-dropped **PII** and **anonymization** features.
  Drop all remaining artifacts in code, schema and UI.
- **#4** Mail rework: HTML, prettier, more context on the trigger.
  - **#4-1** Mail the applicant on a new comment. Mail all role holders who can act on the
    current state when the applicant comments.
  - **#4-2** A notification settings page reachable from the username dropdown in the header.
    Users configure which notifications they receive.
  - **#4-3** More notification types. Propose the options to the user first.
- **#5** A PWA manifest, so the site installs as an app.
- **#6** More granular global permissions, settable in the role-config admin page.

**Decisions (asked 2026-06-11):**
- #2: Nextcloud-style activity feed — grouped by day, one icon per action category, one
  human-readable sentence (actor + action + target link), expandable details row.
- #3: FULL anonymization feature removal, not only the audit enum artifacts. Delete `anonymize()`
  (applications/service.py:819), the `anonymized_at` column (migration), the `isPII` form-field
  flag, the recipient-resolver skip and the frontend/i18n traces.
- #4: HTML mails = a code base layout (logo, colors, footer with "why you got this" plus a
  settings link) plus the DB MailTemplate inner content per type and language.
- #4-2: preferences all-on by default, opt-out per type. Essential mails (magic-link) stay on.
  Table principal × type, page /account/notifications, link in the username dropdown.
- #4-3: ALL four groups approved: own-application lifecycle (every status change), task
  notifications (state actionable for a role + stale reminders), meeting and vote events (agenda
  published, vote opened or closed), role and delegation events (assigned or revoked).
- #5: FULL @angular/pwa — service worker + manifest + icons, not manifest-only.
- #6: all four splits approved: admin.config → admin.site/admin.gremien/admin.types,
  budget.manage → budget.structure/budget.book, meeting.manage → meeting.manage/protocol.finalize,
  audit.read → audit.read/audit.verify. The migration must remap existing role_permission rows.

Facts found: the EXPORT audit action exists but nothing records it. There are 3 export endpoints
(applications/router.py:242, budget/tree_router.py:99,287). The mail infrastructure has unused
HTML support (MailMessage.html, body_html_i18n). The `comment_added` event exists with no
handler. There is no user-preference table. Account pages live at /account/* (grants, consent),
and the dropdown is in layout/shell.component.html:36-68.

The user insists: ask through the question tool when something is unclear, consult before design
decisions, make no assumptions. See [[work-autonomously]], [[track-side-requests]],
[[admin-domain-rules]].
