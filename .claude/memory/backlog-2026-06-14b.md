---
name: backlog-2026-06-14b
description: "8-item backlog (PWA name, fades, fuzzy search, backend filters, perms, pre-fills, CSS)"
metadata: 
  node_type: memory
  type: project
---

Backlog given 2026-06-14 (priority order). Rule: ASK design decisions per item
([[ask-all-design-decisions]]).

1. PWA name config-driven — currently hardcoded "StuPa"; expose in existing Branding & Texts
   admin page. (PWA manifest name/short_name are static at build → runtime-config approach
   needs deciding.)
2. Tree-filters (left panel): fade content at TOP and BOTTOM edges when scrolling, like the
   meetings-tab timeline.
3. Search in /expenses + /invoices → fuzzy, match EVERY field, sort by match ranking. Add the
   same search line to /applications + /meetings.
4. All filters should also filter in the BACKEND where sensible (avoid overloading FE with
   large data).
5. Add permission: view ALL meetings (view-only).
6. Sensible pre-fills for text inputs:
   6-1. Create-Meeting dialog → 2-step: step 1 date/committee/time, step 2 name pre-filled
        "Sitzung des <committee> am <date>".
   6-2. Vote question (Abstimmungsfrage) for application TOPs pre-filled "Soll der Antrag
        <application-name> wie beschrieben gefördert werden?".
7. Formly checkboxes have more space above than below — equalize.
8. Meetings header: title should align with the grey bar; breadcrumbs move further right
   GLOBALLY so the breadcrumbs' RIGHT edge aligns with the right edge of the site logo.
   (Earlier breadcrumb-align attempts were reverted — see git fc07008.)

Status (2026-06-14):
- #1 DONE (commit a00b356) — backend manifest route + nginx + BrandingService; needs redeploy.
- #2 DONE (dab6715) — fade on .bt__nav + cost-centre-tree :host (space-5 fade, not space-6).
- #7 DONE (2e00af1) — apply-wizard gap:0 + symmetric margin/padding around divider.
- #5 decided: global `meeting.view_all` (read list/detail/agenda/protocol/votes, no manage/write/vote).
- #8a DONE (ff3482f) — .mtg__head--detail max-width+margin-auto, detail view only (list stays left).
- #8b DEFERRED — breadcrumb left edge = logo left edge / mobile fully left. I repeatedly misread the
  desktop header geometry (menu-btn is display:none on desktop → logo is at page edge already);
  needs LIVE inspection or exact px positions before touching. Do NOT guess again.
- #6 DONE (ef49e5a) — 2-step create dialog + name/vote-question prefills.
- #3/#4 DONE — server-side pg_trgm fuzzy search + filters + pagination. Foundation:
  migration 0027_pg_trgm_search (pg_trgm + 13 GIN trgm indexes + app_search_text(jsonb) fn) +
  app/search.py trigram_rank (word_similarity, ILIKE fallback for SQLite). Lists: expenses
  (f9d8963), applications + meetings (f9d8963, collapse-to-relevance when searching),
  invoices (31a73e2, full client→server rewrite). budget = nothing to do.
- ALL backlog items done EXCEPT #8b (deferred). Backend (#1,#5,#3/#4) needs stack rebuild +
  migration 0027. NOTE: `CREATE EXTENSION pg_trgm` needs a DB role with CREATE rights —
  pre-create if the managed Postgres role can't. Nothing pushed.
Commits NOT pushed (user's standing preference: local main, no push).
